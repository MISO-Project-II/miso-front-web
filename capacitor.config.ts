import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'miso.uniandes',
  appName: 'sport-app',
  webDir: 'dist/SportApp/',
  bundledWebRuntime: false
};

export default config;
